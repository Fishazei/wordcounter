apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
spec:
  selector:
    app: {{ .Release.Name }}
  type: NodePort
  ports:
    - port: 8080
      targetPort: 80
      #nodePort: 30080
      # leave blank for now idc fam
